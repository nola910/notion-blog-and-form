---
interface Props {
  id: string;
  title: string;
  summary: string;
  content: string; // HTML or Markdown formatted content
  date: string;
  tags: string[];
  author: string;
}

const { id, title, summary, content, date, tags = [], author = "Medical Director" } = Astro.props;

// Colors for tags
const tagColors = [
  "bg-teal-500/10 text-teal-400 border-teal-500/20",
  "bg-indigo-500/10 text-indigo-400 border-indigo-500/20",
  "bg-amber-500/10 text-amber-400 border-amber-500/20",
  "bg-rose-500/10 text-rose-400 border-rose-500/20",
  "bg-sky-500/10 text-sky-400 border-sky-500/20",
];

const getTagColor = (index: number) => tagColors[index % tagColors.length];
---

<article 
  class="group relative flex flex-col justify-between overflow-hidden rounded-2xl border border-slate-800 bg-slate-900/30 p-6 backdrop-blur-sm transition-all duration-300 hover:border-slate-700/60 hover:bg-slate-900/50 hover:shadow-lg"
>
  <div>
    <!-- Post Meta -->
    <div class="flex items-center justify-between mb-3 text-xs font-semibold text-slate-500">
      <div class="flex items-center gap-1.5">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="h-3.5 w-3.5 text-teal-500">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span>{date}</span>
      </div>
      <span class="text-slate-400 font-medium">By {author}</span>
    </div>

    <!-- Title -->
    <h3 class="font-display text-xl font-bold text-white transition-colors duration-200 group-hover:text-teal-300">
      {title}
    </h3>

    <!-- Tags -->
    {tags.length > 0 && (
      <div class="flex flex-wrap gap-1.5 mt-3">
        {tags.map((tag, i) => (
          <span class={`rounded-full border px-2 py-0.5 text-[10px] uppercase font-bold tracking-wider ${getTagColor(i)}`}>
            {tag}
          </span>
        ))}
      </div>
    )}

    <!-- Summary -->
    <p class="mt-4 text-sm leading-relaxed text-slate-400 line-clamp-3">
      {summary}
    </p>
  </div>

  <!-- Read More Trigger -->
  <div class="mt-6 border-t border-slate-800/80 pt-4 flex justify-between items-center">
    <button 
      class="blog-read-more text-xs font-semibold text-teal-400 hover:text-teal-300 transition-colors flex items-center gap-1"
      data-post-id={id}
    >
      Read Full Update
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="h-3.5 w-3.5 transition-transform duration-200 group-hover:translate-x-0.5">
        <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
      </svg>
    </button>
  </div>

  <!-- Hidden Content for Modal -->
  <template id={`content-${id}`}>
    <div class="prose prose-invert prose-teal max-w-none text-slate-300 text-sm leading-relaxed">
      <!-- Title in modal -->
      <h2 class="text-2xl font-bold text-white font-display mb-2">{title}</h2>
      
      <!-- Meta in modal -->
      <div class="flex flex-wrap items-center gap-4 text-xs font-medium text-slate-500 mb-6 border-b border-slate-800 pb-4">
        <span class="flex items-center gap-1">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="h-3.5 w-3.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5A2.25 2.25 0 0121 11.25v7.5m-9-6h.008v.008H12v-.008zM12 15h.008v.008H12V15zm0 2.25h.008v.008H12v-.008zM9.75 15h.008v.008H9.75V15zm0 2.25h.008v.008H9.75v-.008zM7.5 15h.008v.008H7.5V15zm0 2.25h.008v.008H7.5v-.008zm6.75-4.5h.008v.008h-.008v-.008zm0 2.25h.008v.008h-.008V15zm0 2.25h.008v.008h-.008v-.008zm2.25-4.5h.008v.008H16.5v-.008zm0 2.25h.008v.008H16.5V15z" />
          </svg>
          {date}
        </span>
        <span>By {author}</span>
        <div class="flex gap-1">
          {tags.map((tag, i) => (
            <span class={`rounded-full border px-2 py-0.5 text-[9px] uppercase font-bold tracking-wider ${getTagColor(i)}`}>
              {tag}
            </span>
          ))}
        </div>
      </div>

      <!-- Content body -->
      <div class="space-y-4 whitespace-pre-line text-slate-300">
        {content}
      </div>
    </div>
  </template>
</article>
